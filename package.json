{
  "name": "nested-git-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/lodash": "^4.14.157",
    "@types/node": "^14.0.14",
    "chalk": "^4.1.0",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "husky": {
    "hooks": {
      "pre-push": "(git stash push || exit 0) ; tsc -p . --noEmit ; code=$? ; (git stash pop || exit 0) ; (exit $code)"
    }
  }
}
